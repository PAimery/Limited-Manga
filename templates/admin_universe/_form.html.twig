{{ form_start(form) }}
<div class="h1 fs-4 my-3">{{ form_widget(form) }}</div>
<button class="btn text-dark fs-6 pt-2 px-4 btn-light">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
