{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	{{ encore_entry_link_tags('universe') }}
{% endblock %}

{% block body %}
	<header class="d-flex justify-content-center align-items-center">
		<h1 class="text-primary">Limited Manga</h1>
	</header>
	<div class="text-center my-5">
		<h1 class="text-primary m-0">collectors</h1>
		<p class="text-dark fst-italic fs-6 fw-bold">"Derniers ajoutés à la liste"</p>
	</div>

	<section class="container p-5">
		<div class="row g-4">
			{% for collector in collectors %}
				{# xs sm md lg xl xxl#}
				<div class="col col-lg-6 col-xl-4">
					<a role="button" class="" href="{{ path("collector_show", { id: collector.id }) }}">
						<i class="bi bi-heart"></i>
						<i class="bi bi-heart-fill"></i>
						<div class="card shadow-0 postion-relative rounded-1 bg-transparent border border-0">
							<div class="position-absolute top-0 end-0 text-light bg-dark py-2 px-3 BNSuper fs-5 rounded-1">{{ collector.price }}
								€
							</div>
							<div class="d-flex justify-content-center">
								<img src="{{ collector.image ? asset('/uploads/collector/' ~ collector.image) : asset('build/images/collector.png') }}" class="card-image w-50 " alt=" ...">

							</div>
							<div class="mx-5 my-3">
								<h3 class="BNSuper fs-4 text-dark m-0 text-decoration-none">{{ collector.title }}</h3>
								<h3 class="roboto fs-5 fw-bold text-dark fst-italic p-0 text-decoration-none">{{ collector.type }}
									<span class="fs-5 fw-bolder">{{ collector.number }}</span>
								</h3>
							</div>
						</div>
					</a>
				</div>
			{% else %}
				<h3 class="roboto fw-bolder fs-5 m-5">Aucun collector trouvé.</h3>
			{% endfor %}
		</div>
	</section>
	<section class="bg-dark py-5">
		<div class=" banniere container d-flex flex-row align-items-center justify-content-center">
			<div>
				<h3 class="fs-1 text-light BNSuper m-2">Suivez-moi !</h3>
			</div>
			<div>
				<img src="{{ asset('build/images/pp-limited.png') }}" class="m-0 w-75" alt=" ...">
			</div>
			<div class="d-flex flex-column m-2">
				<div class="text-light BNSuper fs-3 m-2">
					{% include 'icons/twitter.svg' %}
					@limitedmangas</div>
				<p class="roboto text-light fw-bold fs-6">
					Compte de référence des manga éditions collectors/limitées/coffrets !
					<br>
					Retrouvez toute l’actualité et les liens de préco !
					<br>
					📖 Thread «collector» chaque mois !</p>
				<a type="button" class="btn btn-outline-light BNSuper fs-4 col-4 text-center" href="https://twitter.com/LimitedMangas">FOLLOW</a>
			</div>
		</div>
	</section>
	<div class="text-center my-5">
		<h1 class="text-primary m-0">univers</h1>
		<p class="text-dark fst-italic fs-6 fw-bold">"Récemment ajoutés à la liste"</p>
	</div>
	<section class="container">
		<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 row-cols-xl-5 g-3">
			{% for universe in universes %}
				{# xs sm md lg xl xxl#}
				<div class="col">
					<a role="button" class="" href="{{ path("universe_show", { id: universe.id }) }}">

						<div class="cardUniverse px-1 py-5 d-flex justify-content-center align-items-center rounded-1 bg-">
							<img src="{{ universe.image ? asset('/uploads/universe/' ~ universe.image) : asset('build/images/logoTorii.png') }}" class="card-image w-50" alt=" ...">
						</div>
						<div class="d-flex justify-content-center">
							<a role="button" class="btn BNSuper text-uppercase text-center text-dark p-2 fs-5">{{ universe.title }}</a>
						</div>
					</div>
				</a>

			{% else %}
				Aucun univer trouvé.
			{% endfor %}
		</div>
		<div class="d-flex justify-content-center">
			<a type="button" class="btn btn-outline-dark  BNSuper fs-4 px-5 py-2 text-center shadow m-5" href="{{ path('universe_index') }}">découvrir</a>
		</div>
	</section>
{% endblock %}
