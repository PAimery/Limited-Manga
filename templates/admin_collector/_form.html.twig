{{ form_start(form) }}
<div class="h2 fs-5 my-3">{{ form_widget(form) }}</div>
<button class="btn btn-outline-dark fs-5 px-4 border border-0 mt-4 mx-3">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
